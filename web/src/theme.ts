import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4066AB',
    },
    secondary: {
      main: '#D14526',
    },
    background: {
      default: '#EFEFEF',
    },
  },
});

export default theme;
